---
// Technologies page temporarily hidden. Restore by replacing with original content.
export async function GET() {
    return new Response(null, {
        status: 302,
        headers: { Location: "/" },
    });
}

